# Quickstart

Install the library and create a client.

```ts
import { createClient } from "example-lib";

const client = createClient({ apiKey: process.env.EXAMPLE_KEY });
```

Next: see [Authentication](./api/auth) and [Webhooks](./api/webhooks).
